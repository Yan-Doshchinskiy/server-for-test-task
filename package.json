{
  "name": "boilerplate",
  "version": "3.0.0",
  "engines": {
    "node": "12.x"
  },
  "scripts": {
    "start": "cd client && yarn run start",
    // "build": "cd client/ && yarn install && yarn run build",
    "build": "node server && cd client && yarn run start",
    "dev": "node server && cd client && yarn run start"
  },
  "cacheDirectories": [
    "node_modules",
    "client/node_modules"
  ],
  "dependencies": {
    "express": "^4.17.1",
    "nodemon": "^2.0.7"
  },
  "devDependencies": {},
  "main": "index.js",
  "repository": "https://github.com/CallbackAttack/redux-boiler",
  "author": "yan.doshchinskiy",
  "license": "MIT",
  "private": false
}
